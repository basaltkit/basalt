{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "pnpm docs:packages && vitepress build",
    "docs:preview": "vitepress preview",
    "docs:api": "node scripts/gen-typedoc.mjs && typedoc --options typedoc.json && node scripts/sanitize-api.mjs",
    "docs:packages": "node scripts/gen-packages.mjs",
    "docs:build:full": "pnpm docs:api && pnpm docs:packages && vitepress build"
  },
  "devDependencies": {
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0",
    "typedoc-vitepress-theme": "^1.1.3",
    "vitepress": "^1.6.3"
  }
}
