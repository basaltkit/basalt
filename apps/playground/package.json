{
  "name": "playground",
  "private": true,
  "description": "Basalt reference app — integrates core, config, env, logger, events and fastify. Serves as the living E2E of Phase 1.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@basaltkit/config": "workspace:*",
    "@basaltkit/core": "workspace:*",
    "@basaltkit/env": "workspace:*",
    "@basaltkit/events": "workspace:*",
    "@basaltkit/fastify": "workspace:*",
    "@basaltkit/logger": "workspace:*",
    "@basaltkit/tenancy": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@basaltkit/tsconfig": "workspace:*",
    "@types/node": "^22.15.0",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
