{
  "name": "playground",
  "private": true,
  "description": "Basalt reference app \u2014 the same neutral route() list served on fastify, express AND hono (ADAPTER=env), also exposed as MCP tools. Living E2E of the core (config, env, logger, events, tenancy, HTTP, MCP).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "mcp:stdio": "tsx src/mcp-stdio.ts"
  },
  "dependencies": {
    "@basaltkit/config": "workspace:*",
    "@basaltkit/core": "workspace:*",
    "@basaltkit/env": "workspace:*",
    "@basaltkit/events": "workspace:*",
    "@basaltkit/express": "workspace:*",
    "@basaltkit/fastify": "workspace:*",
    "@basaltkit/hono": "workspace:*",
    "@basaltkit/http": "workspace:*",
    "@basaltkit/logger": "workspace:*",
    "@basaltkit/mcp": "workspace:*",
    "@basaltkit/tenancy": "workspace:*",
    "@hono/node-server": "^2.1.1",
    "zod": "^3.24.0 || ^4.0.0"
  },
  "devDependencies": {
    "@basaltkit/tsconfig": "workspace:*",
    "@types/node": "^26.4.0",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
